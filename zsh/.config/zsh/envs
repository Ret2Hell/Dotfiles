export EDITOR="nvim"
export SUDO_EDITOR="$EDITOR"

export PATH="$HOME/.cache/.bun/bin:$PATH:$HOME/.local/bin"

export BAT_THEME=ansi
export MANROFFOPT="-c"
export MANPAGER="sh -c 'col -bx | bat -l man -p'"

# Ensure mise works (disable command hashing)
setopt NO_HASH_CMDS
setopt NO_HASH_DIRS
