# If not running interactively, don't do anything
[[ $- != *i* ]] && return

source "$HOME/.config/zsh/envs"
source "$HOME/.config/zsh/aliases"
source "$HOME/.config/zsh/functions"
source "$HOME/.config/zsh/inits"
